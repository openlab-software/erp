FROM alpine:latest

RUN apk --no-cache add ca-certificates tzdata && \
    addgroup -S app && adduser -S app -G app

WORKDIR /app
COPY apps/stock-service/bin/api* .

USER app
EXPOSE 8081
ENTRYPOINT ["./api"]
